<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aishanyasingh.steller">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Launcher2Activity" />
        <activity android:name=".createAccountActivity" />
        <activity android:name=".storyBoardActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".readActivity" />
        <activity android:name=".writeActivity" />
        <activity android:name=".write2Activity" />
        <activity android:name=".readByMoodActivity" />
        <activity android:name=".readByPenNameActivity" />
        <activity android:name=".myProfileActivity" />
        <activity android:name=".storyBoxActivity" />
        <activity android:name=".enterMessageActivity" />
        <activity android:name=".userProfileActivity"></activity>
    </application>

</manifest>